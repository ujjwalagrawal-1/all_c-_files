#include <iostream>
#include <vector>
using namespace std;

int main (){
    /*
    vector<int>arr;
    //Another way to find the array size
    //int x = (sizeof(arr)/sizeof(int));
    //cout<<x<<endl;
    cout<<arr.size()<<endl;
    cout<<arr.capacity()<<endl;
    arr.push_back(5);
    arr.push_back(8);
    
    cout<<arr.size()<<endl;
    for(int i = 0 ; i<arr.size() ; i++){
        cout<<arr[i]<<" ";
    }
    */
   // pop_back();//How to use don't Know ..
   //////////////////////////////////////                                new                           ///////////////////////////




   /*
   vector<int> arr(10,2);
   cout<<arr.size()<<" "<<endl;
   for(int i = 0 ; i<arr.size() ; i++){
        cout<<arr[i]<<" ";
    }
 */ 



//////////////////////////////////////                              New Way to Declare Vector                     /////////////////////////////////////
vector<int>arr{10,20,3,0,4,0};
for(int i = 0 ; i<arr.size() ; i++){
        cout<<arr[i]<<" ";
    }
    for(int j)
}